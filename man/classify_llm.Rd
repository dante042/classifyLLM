% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_llm.R
\name{classify_llm}
\alias{classify_llm}
\title{Classify a character vector into predefined categories using an LLM}
\usage{
classify_llm(
  x,
  categories,
  model = "gpt-4o-mini",
  temperature = 0,
  batch_size = 1L,
  delay = 0,
  verbose = FALSE
)
}
\arguments{
\item{x}{Character vector to classify.}

\item{categories}{Character vector of allowed categories (labels).}

\item{model}{OpenAI model name. Default: "gpt-4o-mini".}

\item{temperature}{Numeric in \link{0,1}; default 0 for deterministic outputs.}

\item{batch_size}{Integer; number of elements per API call batch. Default 1.}

\item{delay}{Seconds to sleep between batches to avoid rate limits. Default 0.}

\item{verbose}{Logical; print progress info. Default FALSE.}
}
\value{
A factor with levels = \code{categories}, aligned to input length.
}
\description{
Designed for tidyverse pipelines. Sends each element of \code{x} to the model
with a strict instruction to return exactly one label from \code{categories}.
}
\examples{
\dontrun{
Sys.setenv(OPENAI_API_KEY = "sk-...")
classify_llm(
  x = c("siamese kitty","golden retriever","parakeet"),
  categories = c("cat","dog","bird")
)
}
}
